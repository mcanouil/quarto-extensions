<%
const excludeCategories = [
  'example', 'template',
  'format', 'formats',
  'filter', 'filters',
  'shortcode', 'shortcodes'
];

// Shared function to get status banner based on categories
// Returns object with type, text, class, and icon
// Priority: superseded > deprecated > archived > experimental > stable
const getStatusBanner = (categories) => {
  const lowerCategories = categories.map(c => c.toLowerCase());

  if (lowerCategories.includes('superseded')) {
    return { type: 'superseded', text: 'Superseded', class: 'warning', icon: 'bi-exclamation-triangle-fill' };
  } else if (lowerCategories.includes('deprecated')) {
    return { type: 'deprecated', text: 'Deprecated', class: 'warning', icon: 'bi-exclamation-triangle-fill' };
  } else if (lowerCategories.includes('archived')) {
    return { type: 'archived', text: 'Archived', class: 'secondary', icon: 'bi-archive-fill' };
  } else if (lowerCategories.includes('experimental')) {
    return { type: 'experimental', text: 'Experimental', class: 'info', icon: 'bi-flask' };
  } else if (lowerCategories.includes('stable')) {
    return { type: 'stable', text: 'Stable', class: 'success', icon: 'bi-check-circle-fill' };
  }
  return null;
};

// Compare two version strings, descending (negative when `a` is newer).
// A pre-release (e.g. 1.10.0-daily) ranks below the matching release.
const compareVersionsDesc = (a, b) => {
  const core = v => String(v || '').split('+')[0].split('-')[0].split('.').map(n => parseInt(n, 10) || 0);
  const pa = core(a), pb = core(b);
  for (let i = 0; i < Math.max(pa.length, pb.length); i++) {
    const diff = (pb[i] || 0) - (pa[i] || 0);
    if (diff !== 0) return diff;
  }
  const suffix = v => String(v || '').split('+')[0].split('-').slice(1).join('-');
  const sa = suffix(a), sb = suffix(b);
  if (sa !== sb) {
    if (!sa) return -1;
    if (!sb) return 1;
    return sb.localeCompare(sa);
  }
  return 0;
};

// Keep the highest-version test result per channel.
const latestTestByChannel = (testResults) => {
  const latest = {};
  (testResults || []).forEach(r => {
    const ch = r['quarto-channel'];
    if (!ch) return;
    if (!latest[ch] || compareVersionsDesc(r['quarto-version'], latest[ch]['quarto-version']) < 0) {
      latest[ch] = r;
    }
  });
  return latest;
};

// Card status from the latest release + latest prerelease only.
const cardTestStatus = (testResults) => {
  if (!testResults || testResults.length === 0) return 'none';
  const latest = latestTestByChannel(testResults);
  const results = ['release', 'prerelease'].map(ch => latest[ch]).filter(Boolean);
  if (results.length === 0) return 'none';
  if (results.some(r => r.status === 'fail')) return 'fail';
  if (results.some(r => r.status === 'pass')) return 'pass';
  return 'skip';
};
%>

:::: {#extensions-listing-container}

:::: {#listing-controls .mb-4}
<%
partial("_controls.ejs")
%>
::::

<!-- Main Content Area -->
:::: {.grid}

<!-- Extensions List -->
::: {.g-col-12}
:::: {#extensions-container}

<!-- Grid View -->
:::: {#grid-view-container .view-container}
<%
partial("_grid.ejs", { items, excludeCategories, utils, getStatusBanner, latestTestByChannel, cardTestStatus })
%>
::::

<!-- List View -->
:::: {#list-view-container .view-container .d-none}
<%
partial("_list.ejs", { items, excludeCategories, utils, getStatusBanner, latestTestByChannel, cardTestStatus })
%>
::::

<!-- Include modals for each extension -->
<% for (const item of items) { %>
<%
const usage = item.usage;
const id = utils.b64encode(usage);
const key = usage.split('@')[0];
const isTemplate = item.template === true;
const isExample = item.example === true;
partial("_modal-install.ejs", { id, usage });
if (isTemplate || isExample) {
  partial("_modal-use.ejs", { id, usage });
}
if (item['test-results'] && item['test-results'].length > 0) {
  partial("_modal-test-history.ejs", { id, key, testResults: item['test-results'], compareVersionsDesc });
}
%>
<% } %>

::::
:::
::::

```{=html}
<style>
.list-group-item-action:hover, .list-group-item-action:focus {
  color: unset;
  background-color: unset;
}
.extension-card {
  border-color: var(--bs-border-colour, #dee2e6);
}
.extension-card .btn:hover,
.extension-item .btn:hover,
.modal .btn:hover {
  transform: none;
}
.modal .btn-group {
  width: 100%;
}
.card-footer {
  border-color: var(--bs-border-colour, #dee2e6);
}
.list-group-item.extension-item {
  border-top: 1px solid var(--bs-border-colour, #dee2e6);
}
.list-group-item.extension-item:not([hidden]):first-of-type {
  border-top: 1px solid var(--bs-border-colour, #dee2e6);
}

.status-banner {
  font-size: 0.875rem;
  border: none;
  margin: 0;
}
.status-banner i {
  font-size: 0.875rem;
}

.btn-group.flex-wrap {
  flex-wrap: wrap !important;
  
  .btn {
    flex-shrink: 1;
    min-width: 0;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }
}

.filter-chip {
  min-width: 2.5rem;
  
  @media (max-width: 576px) {
    font-size: 0.75rem;
    padding: 0.25rem 0.5rem;
  }
}

.quick-filters {
  .d-flex {
    max-width: 100%;
    overflow-x: auto;
    -webkit-overflow-scrolling: touch;
    scrollbar-width: none;
    -ms-overflow-style: none;
    
    &::-webkit-scrollbar {
      display: none;
    }
  }
}

.test-status-badges-row {
  min-height: 0;
}
.test-badge-btn {
  font-size: 0.75rem;
  line-height: 1.2;
  display: inline-flex;
  align-items: center;
  gap: 0.25rem;
  border: none;
  background: none;
  cursor: pointer;
  padding: 0.125rem 0.25rem;
  border-radius: 0.25rem;
  transition: background-color 0.15s ease-in-out;
}
.test-badge-btn:hover {
  background-color: rgba(0, 0, 0, 0.1);
  transform: scale(1.05);
}
[data-bs-theme="dark"] .test-badge-btn:hover {
  background-color: rgba(255, 255, 255, 0.15);
}
.test-badge-btn i {
  font-size: 0.8rem;
}
#test-history-modal .table {
  font-size: 0.875rem;
}
#test-history-modal code {
  font-size: 0.8rem;
}

@media (max-width: 767px) {
  .btn-group {
    max-width: 100%;
    
    .btn {
      font-size: 0.875rem;
      padding: 0.375rem 0.5rem;
    }
  }
  
  // Stack search and view toggle on very small screens
  @media (max-width: 575px) {
    .grid .g-col-12 {
      text-align: center !important;
    }
  }
}
</style>
```

::::
